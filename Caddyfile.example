# Keep Cloud private: publish this listener only over Tailscale, or retain the
# loopback upstream and put it behind a Tailscale-only Caddy listener.
cloud.tailnet-name.ts.net {
  reverse_proxy 127.0.0.1:8787
  header {
    Strict-Transport-Security "max-age=31536000; includeSubDomains"
  }
}
